README for /Assets/ConfigAssets

This directory holds resources that specifiy the currently build product. The content of this directory and its subdirectories will be generated by our Unity Editor extensions or scripts. It should not be checked into the git since it will change frequently and not represent a real advance in the project development. 

hm